apiVersion: apps/v1
kind: Deployment
metadata:
  name: team-a-app
  namespace: team-a
spec:
  replicas: 2
  selector:
    matchLabels:
      app: team-a-app
  template:
    metadata:
      labels:
        app: team-a-app
    spec:
      containers:
        - name: app
          image: ${{ secrets.DOCKERHUB_USERNAME }}/privatemultiuser:team-a-app
          ports:
            - containerPort: 5000
      imagePullSecrets:
        - name: regcred 